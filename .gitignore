# Local Netlify folder
.netlify
.netlify-publish/
.vercel/
.DS_Store
.claude/

# Dependencies
node_modules/
__pycache__/
*.py[cod]

# Secrets — never commit
.env
.env*
*adminsdk*.json
wernotion-firebase-*.json
workspace-api-store.json

# Local backups / one-off migration scripts
firebase-*backup*.json
migrate-to-firebase.mjs

# Local Firebase data exports (contain workspace data — never commit)
firebase-exports/
_wault_firebase_export.json
_tmp_*.mjs
